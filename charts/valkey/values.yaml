## @section Global parameters
global:
  ## @param global.imageRegistry Global Docker Image registry
  imageRegistry: ""
  ## @param global.imagePullSecrets Global Docker registry secret names as an array
  imagePullSecrets: []

## @param clusterDomain Kubernetes cluster domain
clusterDomain: cluster.local

## @section Common parameters
## @param nameOverride String to partially override valkey.fullname
nameOverride: ""
## @param fullnameOverride String to fully override valkey.fullname
fullnameOverride: ""
## @param namespaceOverride String to override the namespace for all resources
namespaceOverride: ""
## @param commonLabels Labels to add to all deployed objects
commonLabels: {}
## @param commonAnnotations Annotations to add to all deployed objects
commonAnnotations: {}

## @section Valkey image configuration
image:
  ## @param image.registry Valkey image registry
  registry: docker.io
  ## @param image.repository Valkey image repository
  repository: valkey/valkey
  ## @param image.tag Valkey image tag (immutable tags are recommended)
  tag: "9.1.0-alpine3.23@sha256:c9b77919daeba2c02ad954d0c844cc4e7142069d177b89c5fd771f405daf9e02"
  ## @param image.imagePullPolicy Valkey image pull policy
  imagePullPolicy: Always

## @section Valkey Architecture
## @param architecture Valkey architecture. Allowed values: standalone, replication
## - standalone: Single Valkey instance
## - replication: Master-replica setup (use sentinel.enabled to enable/disable Sentinel)
architecture: standalone

## @param replicaCount Number of Valkey instances to deploy (only when architecture=replication)
## When using architecture=replication:
##   with Sentinel, this is the total number of Valkey instances (including the initial master)
##   without Sentinel, pod-0 is always the master and other pods are replicas
##   For example: replicaCount: 3 creates 1 master + 2 replicas
replicaCount: 3

## @param revisionHistoryLimit Number of revisions to keep in history for rollback (set to 0 for unlimited)
revisionHistoryLimit: 10

## @param ipFamily IP family to use for replica and sentinel announce IPs. Allowed values: auto, ipv4, ipv6
## auto: Uses the first IP returned by hostname -i (default)
## ipv4: Forces IPv4 address selection
## ipv6: Forces IPv6 address selection
ipFamily: auto

## @section Pod annotations and labels
## @param podAnnotations Map of annotations to add to the pods
podAnnotations: {}
## @param podLabels Map of labels to add to the pods
podLabels: {}

## @section Security Context
podSecurityContext:
  ## @param podSecurityContext.fsGroup Group ID for the volumes of the pod
  fsGroup: 1000

containerSecurityContext:
  ## @param containerSecurityContext.allowPrivilegeEscalation Enable container privilege escalation
  allowPrivilegeEscalation: false
  ## @param containerSecurityContext.runAsNonRoot Configure the container to run as a non-root user
  runAsNonRoot: true
  ## @param containerSecurityContext.runAsUser User ID for the Valkey container
  runAsUser: 999
  ## @param containerSecurityContext.runAsGroup Group ID for the Valkey container
  runAsGroup: 1000
  ## @param containerSecurityContext.readOnlyRootFilesystem Mount container root filesystem as read-only
  readOnlyRootFilesystem: true
  ## @param containerSecurityContext.capabilities.drop Linux capabilities to be dropped
  capabilities:
    drop:
      - ALL

## @param priorityClassName Priority class for the valkey instance
priorityClassName: ""

## @section Valkey Authentication
auth:
  ## @param auth.enabled Enable password authentication
  enabled: true
  ## @param auth.password Valkey password
  password: ""
  ## @param auth.existingSecret The name of an existing secret with Valkey credentials
  existingSecret: ""
  ## @param auth.existingSecretPasswordKey Password key to be retrieved from existing secret
  existingSecretPasswordKey: "password"

## @section TLS/SSL configuration
tls:
  ## @param tls.enabled Enable TLS/SSL for Valkey connections
  enabled: false
  ## @param tls.existingSecret Name of an existing secret containing TLS certificates
  ## Expected keys in the secret: tls.crt, tls.key, ca.crt
  existingSecret: ""
  ## @param tls.certFilename Certificate filename in the secret
  certFilename: "tls.crt"
  ## @param tls.certKeyFilename Certificate key filename in the secret
  certKeyFilename: "tls.key"
  ## @param tls.certCAFilename CA certificate filename in the secret
  certCAFilename: "ca.crt"
  ## @param tls.port TLS port for Valkey (default: 6380)
  port: 6380
  ## @param tls.authClients Require clients to authenticate with a valid client certificate
  authClients: true

## @section Valkey Configuration
config:
  ## @param config.mountPath Redis configuration options
  mountPath: /etc/valkey
  ## @param config.maxMemory Maximum memory usage for Valkey (e.g., 256mb, 1gb)
  maxMemory: ""
  ## @param config.maxMemoryPolicy Memory eviction policy when maxmemory is reached
  maxMemoryPolicy: "allkeys-lru"
  ## @param config.save Valkey save configuration (e.g., "900 1 300 10 60 10000")
  save: "900 1 300 10 60 10000"
  ## @param config.extraConfig Additional Valkey configuration parameters
  extraConfig: []
  ## @param config.existingConfigmap Name of existing ConfigMap with Valkey configuration
  existingConfigmap: ""

## @section External Replica Configuration
## Configure this Valkey instance as a replica of an external Redis/Valkey server
## IMPORTANT: External replica mode is mutually exclusive with Sentinel (sentinel.enabled must be false)
## This feature allows you to replicate data from an external Redis/Valkey server, not from internal cluster members
externalReplica:
  ## @param externalReplica.enabled Enable replication from an external Redis/Valkey server
  ## IMPORTANT: Cannot be used with sentinel.enabled=true
  enabled: false
  ## @param externalReplica.host Hostname or IP address of the external Redis/Valkey master server
  host: ""
  ## @param externalReplica.port Port of the external Redis/Valkey master server
  port: 6379
  ## @param externalReplica.auth.enabled Enable authentication to the external master server
  auth:
    enabled: false
    ## @param externalReplica.auth.existingSecret Name of existing secret containing the password for the external master
    ## The secret should contain a key with the password value
    existingSecret: ""
    ## @param externalReplica.auth.existingSecretPasswordKey Key in the existing secret that contains the password
    existingSecretPasswordKey: "password"

## @section Service configuration
service:
  ## @param service.type Valkey service type
  type: ClusterIP
  ## @param service.port Valkey service port
  port: 6379
  ## @param service.targetPort Valkey container port
  targetPort: 6379
  ## @param service.annotations Service annotations
  annotations: {}

## @section Service Account configuration
## @param serviceAccount.create Enable creation of a Service Account
serviceAccount:
  create: false
  ## @param serviceAccount.name Name of the Service Account
  name: ""
  ## @param serviceAccount.annotations Annotations to add to the Service Account
  annotations: {}
  ## @param serviceAccount.automountToken Enable automounting of the Service Account token
  automountToken: false

## @section Ingress configuration
ingress:
  ## @param ingress.enabled Enable ingress record generation for Valkey
  enabled: false
  ## @param ingress.className IngressClass that will be used to implement the Ingress
  className: ""
  ## @param ingress.annotations Additional annotations for the Ingress resource
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  ## @param ingress.hosts[0].host Hostname for Valkey ingress
  ## @param ingress.hosts[0].paths[0].path Path for Valkey ingress
  ## @param ingress.hosts[0].paths[0].pathType Path type for Valkey ingress
  hosts:
    - host: valkey.local
      paths:
        - path: /
          pathType: Prefix
  ## @param ingress.tls TLS configuration for Valkey ingress
  tls: []
  #  - secretName: valkey-tls
  #    hosts:
  #      - valkey.local

## @section Gateway API parameters
## TLS termination happens at the Gateway listener level, not at the backend service.
## To serve traffic from an HTTPS listener, set sectionName to the listener's name:
##
##   parentRefs:
##     - name: my-gateway
##       sectionName: https   # matches the HTTPS listener defined in the Gateway resource
##
## The backend always receives plain HTTP traffic regardless of the listener protocol.
gatewayAPI:
  httpRoute:
    ## @param gatewayAPI.httpRoute.enabled Enable Gateway API HTTPRoute generation for Valkey
    enabled: false
    ## @param gatewayAPI.httpRoute.annotations Additional annotations for the HTTPRoute resource
    annotations: {}
    ## @param gatewayAPI.httpRoute.parentRefs References to the parent Gateways
    parentRefs:
      - name: gateway
        namespace: ""
        sectionName: ""
    ## @param gatewayAPI.httpRoute.hostnames List of hostnames to match
    hostnames:
      - valkey.local
    ## @param gatewayAPI.httpRoute.rules HTTPRoute rules
    rules:
      - matches:
          - path:
              type: PathPrefix
              value: /

## @section Resources
resources: {}
  # limits:
  #   memory: 64Mi
  # requests:
  #   cpu: 10m
  #   memory: 64Mi

## @section Pod Disruption Budget
pdb:
  ## @param pdb.enabled Enable Pod Disruption Budget
  enabled: false
  ## @param pdb.minAvailable Minimum number/percentage of pods that should remain scheduled
  minAvailable: 1
  ## @param pdb.maxUnavailable Maximum number/percentage of pods that may be made unavailable
  maxUnavailable: ""

## @section Persistence
persistence:
  ## @param persistence.enabled Enable persistence using Persistent Volume Claims
  enabled: true
  ## @param persistence.existingClaim Name of an existing PersistentVolumeClaim to use for Valkey data volume
  existingClaim: ""
  ## @param persistence.storageClass Persistent Volume storage class
  storageClass: ""
  ## @param persistence.annotations Persistent Volume Claim annotations
  annotations: {}
  ## @param persistence.labels Labels for persistent volume claims
  labels: {}
  ## @param persistence.size Persistent Volume size
  size: 8Gi
  ## @param persistence.accessModes Persistent Volume access modes
  accessModes:
    - ReadWriteOnce

## @section Persistent Volume Claim Retention Policy
persistentVolumeClaimRetentionPolicy:
  ## @param persistentVolumeClaimRetentionPolicy.enabled Enable Persistent volume retention policy for the StatefulSet
  enabled: false
  ## @param persistentVolumeClaimRetentionPolicy.whenScaled Volume retention behavior when the replica count of the StatefulSet is reduced
  whenScaled: Retain
  ## @param persistentVolumeClaimRetentionPolicy.whenDeleted Volume retention behavior that applies when the StatefulSet is deleted
  whenDeleted: Retain

## @section Liveness and readiness probes
livenessProbe:
  ## @param livenessProbe.enabled Enable livenessProbe on Valkey containers
  enabled: true
  ## @param livenessProbe.initialDelaySeconds Initial delay seconds for livenessProbe
  initialDelaySeconds: 20
  ## @param livenessProbe.periodSeconds Period seconds for livenessProbe
  periodSeconds: 10
  ## @param livenessProbe.timeoutSeconds Timeout seconds for livenessProbe
  timeoutSeconds: 5
  ## @param livenessProbe.failureThreshold Failure threshold for livenessProbe
  failureThreshold: 3
  ## @param livenessProbe.successThreshold Success threshold for livenessProbe
  successThreshold: 1

readinessProbe:
  ## @param readinessProbe.enabled Enable readinessProbe on Valkey containers
  enabled: true
  ## @param readinessProbe.initialDelaySeconds Initial delay seconds for readinessProbe
  initialDelaySeconds: 5
  ## @param readinessProbe.periodSeconds Period seconds for readinessProbe
  periodSeconds: 5
  ## @param readinessProbe.timeoutSeconds Timeout seconds for readinessProbe
  timeoutSeconds: 1
  ## @param readinessProbe.failureThreshold Failure threshold for readinessProbe
  failureThreshold: 3
  ## @param readinessProbe.successThreshold Success threshold for readinessProbe
  successThreshold: 1

startupProbe:
  ## @param startupProbe.enabled Enable startupProbe on Valkey containers
  enabled: false
  ## @param startupProbe.initialDelaySeconds Initial delay seconds for startupProbe
  initialDelaySeconds: 20
  ## @param startupProbe.periodSeconds Period seconds for startupProbe
  periodSeconds: 5
  ## @param startupProbe.timeoutSeconds Timeout seconds for startupProbe
  timeoutSeconds: 1
  ## @param startupProbe.failureThreshold Failure threshold for startupProbe
  failureThreshold: 30
  ## @param startupProbe.successThreshold Success threshold for startupProbe
  successThreshold: 1

## @param terminationGracePeriodSeconds Seconds Kubernetes waits for the pod to terminate gracefully
## When Sentinel is enabled this must be high enough for the preStop failover to
## finish (the hook can take ~45s: 20s failover wait + Sentinel preStop wait).
## Recommended: 60 for Sentinel setups.
terminationGracePeriodSeconds: 30

## @section Node Selection
## @param nodeSelector Node labels for pod assignment
nodeSelector: {}

## @param tolerations Toleration labels for pod assignment
tolerations: []

## @param affinity Affinity settings for pod assignment
affinity: {}

## @param topologySpreadConstraints Topology Spread Constraints for pod assignment
topologySpreadConstraints: []

## @param hostAliases Pod-level hostAliases (`spec.template.spec.hostAliases`).
## Useful on IPv6-only clusters where BusyBox `hostname -i` cannot resolve the
## pod's own name; injecting a loopback alias for each pod hostname makes the
## init script tolerate `set -e`. Each entry is `{ ip, hostnames: [] }`.
hostAliases: []
  # - ip: "127.0.0.1"
  #   hostnames:
  #     - "valkey-0"
  #     - "valkey-1"

## @param extraInitContainers Additional init containers to add to the Valkey StatefulSet pods
extraInitContainers: []
  # - name: init-sysctl
  #   image: busybox:1.37
  #   command: ["sh", "-c", "echo init"]

## @param extraEnvVars Additional environment variables to set
extraEnvVars: []
  # - name: CUSTOM_VAR
  #   value: "custom-value"
  # - name: SECRET_VAR
  #   valueFrom:
  #     secretKeyRef:
  #       name: my-secret
  #       key: secret-key

## @param extraObjects Array of extra objects to deploy with the release
extraObjects: []
# - apiVersion: v1
#   kind: ConfigMap
#   metadata:
#     name: extra-config
#     namespace: "{{ .Release.Namespace }}"
#   data:
#     key: value

## @section Metrics configuration
metrics:
  ## @param metrics.enabled Start a sidecar prometheus exporter to expose Valkey metrics
  enabled: false
  image:
    ## @param metrics.image.registry Valkey exporter image registry
    registry: docker.io
    ## @param metrics.image.repository Valkey exporter image repository
    repository: oliver006/redis_exporter
    ## @param metrics.image.tag Valkey exporter image tag
    tag: "v1.91.0-alpine@sha256:d294f1b5b974f6daa9880ac42d00ba04cc9999b4c3eb57fee3b75dd7ff4eb25f"
    ## @param metrics.image.pullPolicy Valkey exporter image pull policy
    pullPolicy: Always
  ## @param metrics.resources Resource limits and requests for metrics container
  resources: {}
  ## @param metrics.extraEnvVars Additional environment variables to set for the metrics exporter container
  ## Can be used to pass redis_exporter command line flags as environment variables, e.g.:
  ## extraEnvVars:
  ##   - name: REDIS_EXPORTER_CHECK_KEYS
  ##     value: "db0=mykey*"
  ##   - name: REDIS_EXPORTER_IS_CLUSTER
  ##     value: "true"
  extraEnvVars: []
  ## Metrics service configuration
  service:
    ## @param metrics.service.annotations Additional custom annotations for Metrics service
    annotations: {}
    ## @param metrics.service.labels Additional custom labels for Metrics service
    labels: {}
    ## @param metrics.service.port Metrics service port
    port: 9121
  ## Prometheus Operator ServiceMonitor configuration
  serviceMonitor:
    ## @param metrics.serviceMonitor.enabled Create ServiceMonitor resource(s) for scraping metrics using PrometheusOperator
    enabled: false
    ## @param metrics.serviceMonitor.namespace The namespace in which the ServiceMonitor will be created
    namespace: ""
    ## @param metrics.serviceMonitor.interval The interval at which metrics should be scraped
    interval: 30s
    ## @param metrics.serviceMonitor.scrapeTimeout The timeout after which the scrape is ended
    scrapeTimeout: 10s
    ## @param metrics.serviceMonitor.selector Additional labels for ServiceMonitor resource
    selector: {}
    ## @param metrics.serviceMonitor.annotations ServiceMonitor annotations
    annotations: {}
    ## @param metrics.serviceMonitor.honorLabels honorLabels chooses the metric's labels on collisions with target labels
    honorLabels: false
    ## @param metrics.serviceMonitor.relabelings ServiceMonitor relabel configs to apply to samples before scraping
    relabelings: []
    ## @param metrics.serviceMonitor.metricRelabelings ServiceMonitor metricRelabelings configs to apply to samples before ingestion
    metricRelabelings: []
    ## @param metrics.serviceMonitor.namespaceSelector ServiceMonitor namespace selector
    namespaceSelector: {}

## @section Valkey Sentinel configuration
## This section configures Valkey Sentinel for high availability in replication mode
## When enabled, Valkey instances use dynamic master/replica role assignment managed by Sentinel
## When disabled with replication architecture, pod-0 is always the master and other pods are replicas
sentinel:
  ## @param sentinel.enabled Enable Valkey Sentinel for high availability
  ## IMPORTANT: When enabled, applications should use Sentinel-aware clients to discover the current master
  ## When disabled, pod-0 is the master and can be accessed via the -master service
  enabled: false
  image:
    ## @param sentinel.image.registry Valkey Sentinel image registry
    registry: docker.io
    ## @param sentinel.image.repository Valkey Sentinel image repository
    repository: valkey/valkey
    ## @param sentinel.image.tag Valkey Sentinel image tag
    tag: "9.1.0-alpine3.23@sha256:c9b77919daeba2c02ad954d0c844cc4e7142069d177b89c5fd771f405daf9e02"
    ## @param sentinel.image.pullPolicy Valkey Sentinel image pull policy
    pullPolicy: Always
  ## @param sentinel.config.announceHostnames Use the hostnames instead of the IP in "announce-ip" commands
  config:
    announceHostnames: true
  ## @param sentinel.masterName Name of the master server (default: mymaster)
  masterName: mymaster
  ## @param sentinel.quorum Number of Sentinels that need to agree about the fact the master is not reachable
  quorum: 2
  ## @param sentinel.downAfterMilliseconds Time in milliseconds after the master is declared down
  downAfterMilliseconds: 1500
  ## @param sentinel.failoverTimeout Timeout for failover in milliseconds
  failoverTimeout: 15000
  ## @param sentinel.parallelSyncs Number of replicas that can be reconfigured to use the new master during a failover
  parallelSyncs: 1
  ## @param sentinel.port Sentinel port
  port: 26379
  ## @param sentinel.extraVolumeMounts Additional volume mounts to add to the Sentinel container
  extraVolumeMounts: []
  service:
    ## @param sentinel.service.type Kubernetes service type for Sentinel
    type: ClusterIP
    ## @param sentinel.service.port Sentinel service port
    port: 26379
  ## @param sentinel.resources Resource limits and requests for Sentinel pods
  resources: {}
    # limits:
    #   memory: 128Mi
    # requests:
    #   cpu: 25m
    #   memory: 64Mi
  ## @param sentinel.valkeyShutdownWaitFailover Whether the Valkey container should wait for Sentinel failover before shutting down
  ## When true: on master termination the preStop hook triggers a failover and
  ## waits for it to complete (graceful, near zero-downtime handoff).
  ## When false: the Valkey container shuts down immediately without waiting.
  valkeyShutdownWaitFailover: true
  ## Sentinel container preStop hook for zero-downtime rolling updates.
  ## Keeps the local Sentinel alive until failover completes so clients can still
  ## use it to discover the new master while the pod is terminating.
  preStop:
    ## @param sentinel.preStop.enabled Enable the preStop hook on the Sentinel container
    enabled: true
  masterProxy:
    ## @param sentinel.masterProxy.enabled Enable a stable master endpoint for non-Sentinel-aware clients
    ## Adds an HAProxy sidecar to every Valkey pod (alongside the existing Sentinel sidecar) that
    ## determines the master by checking INFO replication on each backend directly, instead of
    ## trusting a Kubernetes Service selector patched from Sentinel's opinion. It also closes
    ## client connections to a demoted master, forcing an immediate reconnect instead of leaving
    ## clients writing against a stale socket. Because it's bundled into the StatefulSet, proxy
    ## redundancy scales automatically with replicaCount and needs no separate RBAC, ServiceAccount,
    ## affinity, or PodDisruptionBudget.
    enabled: false
    image:
      ## @param sentinel.masterProxy.image.registry HAProxy image registry
      registry: docker.io
      ## @param sentinel.masterProxy.image.repository HAProxy image repository
      repository: haproxy
      ## @param sentinel.masterProxy.image.tag HAProxy image tag
      tag: "3.4-alpine@sha256:c0afc4864dca9c68694cd1290433f0ee79b5c55be80f6745a165ffe373b9a564"
      ## @param sentinel.masterProxy.image.pullPolicy HAProxy image pull policy
      pullPolicy: IfNotPresent
    ## @param sentinel.masterProxy.containerPort Port the HAProxy sidecar listens on
    ## Must differ from service.port, which the Valkey container in the same pod already uses -
    ## enforced at render time with a `fail` if they collide.
    containerPort: 6380
    ## @param sentinel.masterProxy.checkInterval Health check interval (HAProxy `inter`, e.g. "1s")
    checkInterval: "1s"
    ## @param sentinel.masterProxy.checkRise Consecutive successful checks before a backend is marked master-eligible
    checkRise: 2
    ## @param sentinel.masterProxy.checkFall Consecutive failed checks before a backend is marked down
    checkFall: 2
    ## Plain `role:master` is not sufficient on its own: a pod that restarts with empty/ephemeral
    ## storage can come back up self-declared as master (see the sentinel container's bootstrap
    ## logic) before Sentinel has finished failing over, producing two backends that both report
    ## role:master at once - confirmed in testing. To guard against this, the proxy prefers a
    ## candidate with an attached replica and no failover in progress (connected_slaves > 0 and
    ## master_failover_state:no-failover), falling back to plain role:master only when no
    ## candidate satisfies that stricter check - e.g. right after a fresh install, or right after
    ## a replica restarts and is still resyncing, so the endpoint never goes fully unavailable.
    ## This narrows split-brain exposure but doesn't eliminate it: if two candidates satisfy the
    ## stricter check at the same instant (a replica split 1:1 across the old and new master), the
    ## proxy can still round-robin between both until one drops out.
    ## @param sentinel.masterProxy.resources Resource limits and requests for the HAProxy sidecar
    resources: {}
      # limits:
      #   cpu: 100m
      #   memory: 64Mi
      # requests:
      #   cpu: 10m
      #   memory: 32Mi
    service:
      ## @param sentinel.masterProxy.service.type Kubernetes service type for the master proxy service
      type: ""
      ## @param sentinel.masterProxy.service.annotations Additional custom annotations for the master proxy service
      annotations: {}

## @param resources Resource limits and requests for Valkey init container pod
initContainer:
  resources: {}
    # limits:
    #   cpu: 50m
    #   memory: 128Mi
    # requests:
    #   cpu: 25m
    #   memory: 64Mi
